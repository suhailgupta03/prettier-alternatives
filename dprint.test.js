const obj = {
  'simpleKey': 'value',
  'complex key': 'value',
};

// Function with multiple parameters and return statement
function exampleFunction(param1, param2,) {
  const obj = {
    key1: 'value1',
    key2: 'value2',
  };

  if (param1 > param2) {
    return {
      result: 'param1 is greater',
    };
  } else {
    return {
      result: 'param2 is greater or equal',
    };
  }
}

function x(a, b, c, c, d, e, g, h, i, k,) {
  var zz = a + b + c + d + e + g + h + i + k + '..' + '.....' + '.............' + '....................'
    + '...................................';
  return zz;
}

function y() {
  var zx = x([
    1,
    2,
    3,
  ], [
    '..',
  ],);
}

// Array with trailing commas
const array = [
  'item1',
  'item2',
  'item3',
];

// Object with nested objects
const nestedObject = {
  outerKey: {
    innerKey: 'innerValue',
  },
};

// Arrow function
const arrowFunc = (a, b,) => a + b;

// Export statement
export { array, arrowFunc, exampleFunction, nestedObject, };
